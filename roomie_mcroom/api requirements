add new user
    - verify using ucl first.last.yy@ucl.ac.uk
    - set the status as active once the user has clicked the verification code

get room list
    - {room_name, capacity, room_id}

get a specific room avalability -- arguements(datetime.date, room_id)
    - returns a json with available time slots in an array

get occupied status -- argument(datetime.date, room_id)
    - return a json with occupied timeslots in sorted order with the name of the person who booked it

post a time for booking --arguments(datetime.date, room_id, starting_time (datetime.time), end_time (datetime.time))
    - returns a json with success/failure status and possible reasons
        - if success, returns new occupied status of the requested room

get user's all booking
    - returns a list of booking {room, room_id, time_slot, booking id}

post cancel booking {argument: booking id}
    - returns success or failure json

